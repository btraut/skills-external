If you add, remove, or modify a skill, update the README and add an entry to the changelog.

When adding a skill, capture its upstream source in the README.
- Prefer the exact GitHub repository or subdirectory when you can verify it.
- If you cannot verify the source, mark it as `unknown` instead of guessing.
